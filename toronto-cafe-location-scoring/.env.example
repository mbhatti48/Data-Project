# Template. Copy this file to a new file named exactly ".env" and paste your
# real Atlas connection string in. The real .env is gitignored — it never gets committed.
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
